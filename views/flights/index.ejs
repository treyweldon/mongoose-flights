<%- include('../partials/header') %>

  <body>
    <table id="list">
      <thead>
        <tr>
          <th>Airline</th>
          <th>Airport</th>
          <th>Flight #</th>
          <th>Departs</th>
        </tr>
      </thead>
      <tbody>
        <% flights.forEach(function(f) { %>
          <tr>
            <td><%= f.airport %></td>
            <td><%= f.airline %></td>
            <td><%= f.flightNo %></td>
            <td><%= f.departs %></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
  </body>
</html>

<%- include('../partials/footer') %>